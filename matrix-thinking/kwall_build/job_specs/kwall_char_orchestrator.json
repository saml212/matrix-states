{
 "id": "kwall_char_orchestrator",
 "lane": "A",
 "hypothesis": "Under the exact recipe that already brackets a CONVERGED-ROBUST rung (K=24, n=12, indist_min=1.000 uniformly) against a measured 80K-budget non-ROBUST rung (K=32, 0/4 CONVERGED at 1x) -- earlyln free-write, tight-spare d=K+1, encoder hidden h=64 fixed, 80,000 training steps PRIMARY -- does the runner's own conjunctive Gate-1 CONVERGED verdict resolve into additional K-scale structure inside the untested K in {26,28,30} window? (NCR_KWALL_CHARACTERIZATION_DESIGN.md STATUS: RELEASED, commit 1c99cc5, §1/§A11-ADJUDICATION.) This is ONE job: the orchestrator dispatches all up to 16 cell-attempts (12 primary K in {26,28,30} x seed in {0,1,2,3} + up to 4 conditional 160K cells) itself, sequentially, per the design's §4 ORCHESTRATOR CONTRACT -- never 16 separate pool specs (§6 POOL-ELIGIBILITY STATEMENT, F1/§A3-ADJUDICATION: a cumulative budget gate across independently-dispatched cells is a forbidden intra-wave pool dependency; this is why ONE flat spec exists here, not twelve-plus-four).",
 "cmd": "cd /home/nvidia/ncr && /home/nvidia/tdenv/bin/python3 /home/nvidia/ncr/orchestrator.py --primary-outdir /home/nvidia/ncr/results_kwall_characterization --conditional-outdir /home/nvidia/ncr/results_kwall_characterization_160k --smoke-outdir /home/nvidia/ncr/results_kwall_smoke --ledger-path /home/nvidia/ncr/results_kwall_characterization/ORCHESTRATOR_LEDGER.json --report-path /home/nvidia/ncr/results_kwall_characterization/orchestrator_report.json --stop-file /home/nvidia/ncr/results_kwall_characterization/STOP --stop-file-conditional /home/nvidia/ncr/results_kwall_characterization_160k/STOP --python /home/nvidia/tdenv/bin/python3 --cell-runner /home/nvidia/ncr/ncr_earlyln_scale.py --steps-primary 80000 --steps-conditional 160000 --gpu-id ${CUDA_VISIBLE_DEVICES:-0} --git-commit __DEPLOY_GIT_COMMIT__",
 "gpu_h_estimate": 15.50,
 "output_dir": "/home/nvidia/ncr/results_kwall_characterization",
 "validity_check": "cd /home/nvidia/ncr && /home/nvidia/tdenv/bin/python3 -m kwall_lib.validity_check /home/nvidia/ncr/results_kwall_characterization/orchestrator_report.json /home/nvidia/ncr/results_kwall_characterization /home/nvidia/ncr/results_kwall_characterization_160k",
 "notes": "EXCLUSIVE GPU for the whole run duration: this ONE job occupies exactly 1 of the cluster's 8 GPUs for its entire lifetime (nominal ~11.27h, worst-case <=15.50h declared ceiling +<=0.15h micro-smokes = <=15.65h total disclosed spend, design §4/§6) -- it is NEVER co-scheduled with a second job on the SAME GPU index; the other 7 GPUs remain free for concurrent pool work throughout (design §4, 'this occupies exactly 1 of the cluster's 8 GPUs; the other 7 remain free'). Model/activation memory at K in {26,28,30}, d=K+1 in {27,29,31}, h=64 is trivial relative to 80GB H100 VRAM (F(K,d,64) ~= 9.4-11.0M FLOP-scale per the design's own closed-form pricing table -- NOT a VRAM figure, but indicative: this is a tiny toy-scale model, the exclusive-GPU requirement below is about WALL-CLOCK/ceiling accounting exactness (§4's strict-sequential, one-subprocess-in-flight invariant the whole worst-case GPU-h bound depends on), never a memory contention concern. PRECONDITION -- do not promote to queue/jobs/pending/ until: (1) the 3 build-release micro-smokes (K=26/28/30, see smokes/MICRO_SMOKE_SPEC.md) have been run on a GPU verified idle and all read status in {COMPLETED,ABORTED-BUDGET} with K/d/d_override matching K/K+1/K+1 exactly -- their JSONs must already exist at /home/nvidia/ncr/results_kwall_smoke/K{26,28,30}/earlyln_K{K}_s0.json before this job is ever dispatched (the orchestrator's own startup gate re-checks this and refuses to dispatch anything, ledger untouched, if any is missing/failing -- design §4 F3); (2) orchestrator.py + the kwall_lib/ package (this build's deliverables) are deployed to /home/nvidia/ncr/ on the box (NOT done by this build stage -- 'do not touch the box' per the build charter; see BUILD_REPORT.md's REV-1 section, corrected deploy sequence, for the exact deploy commands); (2b) this spec's 'cmd' field still carries the literal placeholder token __DEPLOY_GIT_COMMIT__ in place of a real commit hash (M2 fix, build audit R1 -- the prior '$(git rev-parse HEAD)' command substitution died at argparse if /home/nvidia/ncr is not itself a git working tree, refusing everything before any dispatch); the deploy step MUST replace it with the literal output of 'git rev-parse HEAD' for the commit being deployed (see BUILD_REPORT.md REV-1's deploy sequence, stamping step) BEFORE this spec is ever placed in queue/jobs/pending/ or fallback_pool/ -- a spec still carrying the placeholder is not deploy-ready; (3) the GRID_SHAPES/GRIDS additive patches to ncr_earlyln_scale.py/ncr_task.py (already applied in this repo, matrix-thinking/ncr/) are ALSO deployed to the box's own copies of those two files, additive-only, verified byte-identical elsewhere; (4) the build's own audit + pre-launch resource/placement red-team (design §6 items i-xiv) has passed, per §A11-ADJUDICATION's own staging. --ceiling-gpuh is charged directly by the orchestrator's own ledger at 1.20h/attempt (primary) and 2.32h/attempt (conditional) -- NOT a separate launcher-side figure (design §4, KW4.4 closed); the declared gpu_h_estimate above (15.50) is the SAME number validity_check's own U2 assertion enforces (ledger.realized_gpu_h_final <= 15.50). Resume-safe: re-running this job after a partial run recovers via the orchestrator's own write-ahead ledger + disk reconstruction (design §4 G1, 'never resets realized_gpu_h to 0 ... never leaves a crashed attempt's spend unrecorded past the NEXT restart') -- a requeue costs nothing beyond the ceiling already spent, per queue/QUEUE_README.md's own resume-safety convention, EXCEPT run_status=STOPPED-BY-OPERATOR which is deliberately excluded from validity_check's accept-set and requires a human decision before any resubmission (design G4)."
}
