{
  "id": "130803_ncr_embedpath_compE_s4",
  "lane": "NCR",
  "hypothesis": "DRAFT-R1 compE arm (embed-path intervention wave, matrix-thinking/NCR_EMBED_PATH_DESIGN.md, repo commit db4d385+): assemble_closed_grads_ cuts aux+ortho's MARGINAL gradient contribution into backbone.embed.weight only, AFTER clip_grad_norm_ has already run over the full combined gradient (D-F5) -- entity_adapter stays trainable exactly as compB (mob_g3b31_compB_s1-20, archived median retrieval24_acc@h=61 P1b 0.7246, range [0.6172, 0.9727]). Per R1.1's Stage-0 eval-only pre-test (RE-SCOPE verdict, EXPERIMENT_LOG 2026-08-18 #17): a FULL RESET of either embed OR entity_adapter alone fully rescued retrieval (0.988-1.000) on 4/4 tested compB checkpoints -- the rescue was NOT asymmetric in embed's favor, so the standalone 'embed is the privileged carrier' claim is not supported by that coarse evidence. This wave tests the FINER intervention (removing only the aux/ortho gradient SHARE into embed, not a full reset) against the co-equal compE_adapter arm -- see D-M4/R1.9's pre-registered band ladder (paired vs archived compB s1-s8, median 0.75195) for the read rule. Verified via a Stage-0 gate that the pre-test's own AUTHORIZE band did NOT fire (RE-SCOPE fired instead), so this cell's own result must be read alongside compE_adapter's, never in isolation (R1.9's ADAPTER-LEVERAGE-CONFIRMED / EMBED-SPECIFIC / AMBIGUOUS read rule).",
  "cmd": "mkdir -p /home/nvidia/ncr_embedpath/results && cd /home/nvidia/ncr_embedpath && /home/nvidia/tdenv/bin/python3 ncr_lm_wave1_runner.py --mode calibration --device cuda --steps 20000 --batch-size 32 --eval-batch-size 64 --warmup-steps 200 --lr 3e-4 --aux-read-loss-weight 0.5 --ortho-reg-weight 0.1 --aux-loss-type contrastive+cosine --contrastive-temperature 0.07 --ckpt-every 10000 --eval-every 1000 --ceiling-gpuh 6.0 --cell-id mob_gembed_compE_s4 --seed 4 --close-target embed --min-conduit-ratio 5e-05 --out results/mob_gembed_compE_s4.json --ckpt-dir /ephemeral/embed_path_ckpts/mob_gembed_compE_s4_ckpts 2>&1 | tee -a results/mob_gembed_compE_s4.log",
  "gpu_h_estimate": 0.9843,
  "output_dir": "/home/nvidia/ncr_embedpath/results",
  "validity_check": "/home/nvidia/tdenv/bin/python3 -c \"import json; d=json.load(open('/home/nvidia/ncr_embedpath/results/mob_gembed_compE_s4.json')); assert d.get('status')=='COMPLETED', d.get('status'); assert d.get('step',0)>=20000; assert d.get('config',{}).get('close_target')=='embed', d.get('config',{}).get('close_target')\"",
  "notes": "CANDIDATE -- NOT queue-eligible until audited (this is a BUILD-agent artifact, matrix-thinking/embedpath_build/, per the design's own binding spec DRAFT-R1 in NCR_EMBED_PATH_DESIGN.md and the executed attack round NCR_EMBED_PATH_ATTACK_R1.md, 591301a, REV-REQUIRED 5F/8M/5m, all findings discharged in Rev-1). Runner: patched copy at ~/ncr_embedpath/ncr_lm_wave1_runner.py (md5 d0ba4712f026dfdb00ba14d277a4d486), NOT the pinned file in place (~/ncr_g3b31_contrastive/ncr_lm_wave1_runner.py, md5 9a93198b642242f512ff8489e32b0a53, verified UNTOUCHED after this build). Diff: matrix-thinking/embedpath_build/embed_path_runner.patch. --min-conduit-ratio is REQUIRED by the runner (no silent default, D-A2/R1.15) -- value pinned from a 200-real-training-step box-side smoke measurement, see BUILD_REPORT.md for the full derivation and an explicitly disclosed deviation from DRAFT-R1's own literal 'mean-based' suggestion (measured too variable to be safe as specified -- see the MIN_CONDUIT_RATIO comment in the generator script / BUILD_REPORT.md). Scored by matrix-thinking/embedpath_build/run_repl_wave3.sh (D-F4) against the P1b/h=61/retrieval24_acc regime pinned in R1.2 (D-F1), co-condition median(P1b h=1) >= 0.95, attrition rule n>=7/8 (compE) or n>=3/4 (compE_adapter). Checkpoints to /ephemeral/embed_path_ckpts/ per the disk policy (EXPERIMENT_LOG #4)."
}