{
  "id": "130811_ncr_embedpath_compE_adapter_s12",
  "lane": "NCR",
  "hypothesis": "DRAFT-R1 compE_adapter arm (embed-path intervention wave, matrix-thinking/NCR_EMBED_PATH_DESIGN.md, repo commit db4d385+): assemble_closed_grads_ cuts aux+ortho's MARGINAL gradient contribution into integ.entity_adapter.weight only, AFTER clip_grad_norm_ (D-F5) -- same conduit, same base config as compE/compB, only the CUT TARGET differs. Replaces the causally-absent ncr_head placebo DRAFT-R0 originally proposed (D-M2: ncr_head is off the P1b measurement graph entirely under teacher-forcing -- the scored retrieval24_acc metric is a pure function of ONLY entity_adapter.weight and embed.weight, per pbe_repl.py's own trace). Made MANDATORY (not optional) by R1.1's Stage-0 pre-test: adapter_init (entity_adapter reset to seeded init, embed left at its real trained value) rescued retrieval JUST AS COMPLETELY as embed_init on all 4 tested compB checkpoints (0.996-1.000 vs 0.992-1.000) -- so entity_adapter is at minimum as strong a lever as embed in the coarse (full-reset) regime, and this wave is the first test of whether that holds under the much finer gradient-share cut. See R1.9's directional read rule (n=4, paired against archived compB s9-s12, median 0.7129) -- explicitly NOT a formal separation test at this n.",
  "cmd": "mkdir -p /home/nvidia/ncr_embedpath/results && cd /home/nvidia/ncr_embedpath && /home/nvidia/tdenv/bin/python3 ncr_lm_wave1_runner.py --mode calibration --device cuda --steps 20000 --batch-size 32 --eval-batch-size 64 --warmup-steps 200 --lr 3e-4 --aux-read-loss-weight 0.5 --ortho-reg-weight 0.1 --aux-loss-type contrastive+cosine --contrastive-temperature 0.07 --ckpt-every 10000 --eval-every 1000 --ceiling-gpuh 6.0 --cell-id mob_gembed_compE_adapter_s12 --seed 12 --close-target entity_adapter --min-conduit-ratio 0.049 --out results/mob_gembed_compE_adapter_s12.json --ckpt-dir /ephemeral/embed_path_ckpts/mob_gembed_compE_adapter_s12_ckpts 2>&1 | tee -a results/mob_gembed_compE_adapter_s12.log",
  "gpu_h_estimate": 0.9423,
  "output_dir": "/home/nvidia/ncr_embedpath/results",
  "validity_check": "/home/nvidia/tdenv/bin/python3 -c \"import json; d=json.load(open('/home/nvidia/ncr_embedpath/results/mob_gembed_compE_adapter_s12.json')); assert d.get('status')=='COMPLETED', d.get('status'); assert d.get('step',0)>=20000; assert d.get('config',{}).get('close_target')=='entity_adapter', d.get('config',{}).get('close_target')\"",
  "notes": "CANDIDATE -- NOT queue-eligible until audited (this is a BUILD-agent artifact, matrix-thinking/embedpath_build/, per the design's own binding spec DRAFT-R1 in NCR_EMBED_PATH_DESIGN.md and the executed attack round NCR_EMBED_PATH_ATTACK_R1.md, 591301a, REV-REQUIRED 5F/8M/5m, all findings discharged in Rev-1). Runner: patched copy at ~/ncr_embedpath/ncr_lm_wave1_runner.py (md5 d0ba4712f026dfdb00ba14d277a4d486), NOT the pinned file in place (~/ncr_g3b31_contrastive/ncr_lm_wave1_runner.py, md5 9a93198b642242f512ff8489e32b0a53, verified UNTOUCHED after this build). Diff: matrix-thinking/embedpath_build/embed_path_runner.patch. --min-conduit-ratio is REQUIRED by the runner (no silent default, D-A2/R1.15) -- value pinned from a 200-real-training-step box-side smoke measurement, see BUILD_REPORT.md for the full derivation and an explicitly disclosed deviation from DRAFT-R1's own literal 'mean-based' suggestion (measured too variable to be safe as specified -- see the MIN_CONDUIT_RATIO comment in the generator script / BUILD_REPORT.md). Scored by matrix-thinking/embedpath_build/run_repl_wave3.sh (D-F4) against the P1b/h=61/retrieval24_acc regime pinned in R1.2 (D-F1), co-condition median(P1b h=1) >= 0.95, attrition rule n>=7/8 (compE) or n>=3/4 (compE_adapter). Checkpoints to /ephemeral/embed_path_ckpts/ per the disk policy (EXPERIMENT_LOG #4)."
}