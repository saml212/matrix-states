{
 "id": "0432_ncr_scaleaxis_1b_attrib40k_K40_compB_s2",
 "lane": "NCR",
 "tier": "scaleaxis1b_attribution",
 "hypothesis": "1.31B ATTRIBUTION ARM -- the token-budget control design sec 7.2 makes MANDATORY before any SCALE-DEGRADES verdict is published. Cell: K=40 (d=41), TRAINABLE-contrastive (compB), seed 2, rung 3, RESUMED from its own 20,000-step parent scaleaxis1310m_K40_compB_s2 and extended to 40000 steps at a CONSTANT lr (3.000e-05) -- DOUBLE the token budget at identical params, recipe, seed, pools and ladder. RECIPE-MATCHED per the ruling in DEVIATIONS.md GAP 1: sec 7.2's pinned 'frozen' shape is an artifact of branch (C)'s origin and cannot control a TRAINABLE degradation. CONSTANT-LR IS THE HOUSE MECHANISM (V2', EXPERIMENT_LOG 2026-08-23 #3): the warm-restart variant damaged the 392M K=40 trainable cells (kappa 0.8438 -> 0.5513) and is DEAD. D/N = 0.140 tokens/param at 20k -- the most token-starved point in the whole three-rung curve, so under-training is at its most plausible here and the control is at its most necessary. PRE-REGISTERED BARS: (a) CURVE 1 -- kappa >= 0.90 at h_top=60 on >= 2/3 extended seeds => TOKEN-BUDGET-LIMITED, else SCALE-DEGRADES stands and is STRENGTHENED; (b) V3 ANALOG at s*=13 -- the kappa>=0.90 bar is NOT applicable (sec 5.5(ii): kappa at 13/15 squarings is a numerical-depth reading, not a capability bar), so the criterion is Delta_scale returning inside +-delta_depth = 0.095 vs the matched twin. Both readouts reported for every cell.",
 "cmd": "mkdir -p /ephemeral/scaleaxis1b/attribution/results /ephemeral/scaleaxis1b/attribution/ckpts/attrib1b40k_K40_compB_s2 && [ -f /ephemeral/scaleaxis1b/attribution/ckpts/attrib1b40k_K40_compB_s2/attrib1b40k_K40_compB_s2.ckpt.pt ] || ln /ephemeral/scaleaxis1b/ckpts/scaleaxis1310m_K40_compB_s2/scaleaxis1310m_K40_compB_s2.ckpt.pt /ephemeral/scaleaxis1b/attribution/ckpts/attrib1b40k_K40_compB_s2/attrib1b40k_K40_compB_s2.ckpt.pt && cd /home/nvidia/ncr_scaleaxis && NCR_SCALE=1310m NCR_K=40 /home/nvidia/tdenv/bin/python3 ncr_lm_wave1_runner.py --k 40 --scale 1310m --mode calibration --device cuda --batch-size 32 --eval-batch-size 64 --warmup-steps 200 --lr 3e-4 --aux-read-loss-weight 0.5 --ortho-reg-weight 0.1 --eval-every 1000 --ckpt-every 5000 --steps 40000 --const-lr-on-resume --ceiling-gpuh 45.741 --aux-loss-type contrastive+cosine --contrastive-temperature 0.07 --seed 2 --cell-id attrib1b40k_K40_compB_s2 --out /ephemeral/scaleaxis1b/attribution/results/attrib1b40k_K40_compB_s2.json --ckpt-dir /ephemeral/scaleaxis1b/attribution/ckpts/attrib1b40k_K40_compB_s2 2>&1 | tee -a /ephemeral/scaleaxis1b/attribution/results/attrib1b40k_K40_compB_s2.log",
 "gpu_h_estimate": 15.247,
 "output_dir": "/ephemeral/scaleaxis1b/attribution/results",
 "validity_check": "/home/nvidia/tdenv/bin/python3 -c \"import json, math; d=json.load(open('/ephemeral/scaleaxis1b/attribution/results/attrib1b40k_K40_compB_s2.json')); assert d.get('status')=='COMPLETED', ('status', d.get('status')); assert d.get('step',0)>=40000, ('step', d.get('step')); assert d.get('steps_target')==40000, ('steps_target (TOP-LEVEL, #1)', d.get('steps_target')); assert d.get('runner_tag')=='ncr_scaleaxis_runner_v1', ('runner_tag', d.get('runner_tag')); assert d.get('const_lr_on_resume') is True, ('const_lr_on_resume', d.get('const_lr_on_resume')); assert d.get('resume_start_step')==20000, ('resume_start_step', d.get('resume_start_step')); rc=d.get('resume_const_lr'); assert rc is not None and abs(rc-3e-05)<1e-12, ('resume_const_lr', rc); ks=d.get('kscaling') or {}; assert ks.get('K')==40, ('K', ks.get('K')); assert ks.get('rung')==3, ('rung', ks.get('rung')); assert ks.get('h_top')==60, ('h_top', ks.get('h_top')); assert ks.get('scale')=='1310m', ('scale', ks.get('scale')); bb=ks.get('backbone') or {}; assert (bb.get('d_model'), bb.get('d_state'), bb.get('n_layers'))==(2560,128,22), ('backbone', bb); cfg=d.get('config') or {}; assert cfg.get('freeze_entity_adapter') is False, ('RECIPE MISMATCH -- the arm must match the DEGRADING cell', cfg.get('freeze_entity_adapter')); pp=d.get('params') or {}; assert pp.get('per_arm')==1311527849, ('params.per_arm', pp.get('per_arm')); lh=d.get('loss_history') or {}; assert set(lh)=={'full_graft','backbone_only'}, ('loss_history arms', sorted(lh)); assert len(lh['backbone_only'])>=100, ('backbone_only len', len(lh['backbone_only'])); h=lh['full_graft']; assert len(h)>=100, ('full_graft len', len(h)); assert all(math.isfinite(r[1]) for r in h), 'non-finite CE (Gate-0-marginal, plateau-tolerant per #2)'\"",
 "notes": "CANDIDATE -- NOT queue-eligible, and TRIPLY gated: (a) the 1B sweep must have COMPLETED and produced parent scaleaxis1310m_K40_compB_s2; (b) the pinned cross-scale tests must have ADJUDICATED -- and this wave runs ONLY IF they return SCALE-DEGRADES at this (K, recipe); (c) an envelope ruling, since the 1B ledger already sits far outside sec 8.2's numbers. CONDITIONALITY STATED PLAINLY: these specs presuppose a degradation that NO 1B CELL HAS YET MEASURED. If the 1B trainable cells read SCALE-STABLE there is nothing to attribute and this wave is NOT run -- sec 7.1's one-directionality means STABLE and IMPROVES need no control. They are specced now so the machinery is ready, not because the verdict is assumed. MECHANISM: resume-extension, the design's own pricing basis (sec 7.2 branch (B) and sec 8.2 both price extensions as the MARGINAL 20,000 steps). The parent ckpt is HARDLINKED under a fresh cell id, so atomic_torch_save's os.replace leaves the 20,000-step CHECKPOINT OF RECORD intact and --out points at a NEW json, so the runner's 'already COMPLETED -- skipping' guard cannot fire. CEILING 45.741 GPU-h = 1.5 x the FULL 40000-step projection (30.494 GPU-h at this cell's measured/interpolated 2.7445 s/step). IT MUST COVER THE FULL CUMULATIVE COST: run_two_arm_cell restores cumulative_elapsed_s and checks elapsed against the WHOLE 40,000-step clock, so a ceiling sized on the marginal half fires ABORTED-BUDGET on step 1 of the resume. Gate-0-marginal is SCOPED plateau-tolerant/finite-CE-only per #2; steps_target is asserted at the TOP LEVEL per #1.",
 "attribution1b": {
  "rule": "NCR_SCALE_AXIS_DESIGN.md sec 7.2, recipe-matched per DEVIATIONS.md GAP 1",
  "mechanism": "constant-LR resume (V2' precedent; warm-restart variant DEAD)",
  "parent_cell": "scaleaxis1310m_K40_compB_s2",
  "parent_spec_must_complete_first": true,
  "conditional_on": "a SCALE-DEGRADES verdict at this (K, recipe) -- NOT yet measured",
  "K": 40,
  "recipe": "compB",
  "seed": 2,
  "rung": 3,
  "base_steps": 20000,
  "extended_steps": 40000,
  "token_multiple": 2.0,
  "resume_const_lr": 3e-05,
  "marginal_gpu_h": 15.247,
  "full_40k_gpu_h": 30.494,
  "ceiling_gpuh": 45.741,
  "prereg_curve1": "kappa >= 0.90 at h_top on >= 2/3 seeds => TOKEN-BUDGET-LIMITED",
  "prereg_v3_analog": "Delta_scale at s*=13 inside +-0.095 => TOKEN-BUDGET-LIMITED at depth",
  "queue_eligible": false
 }
}