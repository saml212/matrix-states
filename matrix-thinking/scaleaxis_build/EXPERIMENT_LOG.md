
## 2026-08-25 #2 — Next-wave adjudications: thickening RUNNING (0400-0423 staged zero-gap, ledger corrected 269.0 GPU-h); attribution arm ARMED-CONDITIONAL (0430-0435, 83.2 GPU-h, constant-LR per V2′, runs ONLY if the pinned per-K tests read trainable SCALE-DEGRADES at 1.31B — interim readings 0.8065/0.7877 suggest they will); depth-ext at 1310m CLEARED with one deliberate re-pin ruling (the six-rung driver's PARENT_MD5 pins the kscaling unpatched copy; APPROVED to re-pin to the scaleaxis patched md5 e48eca71 FOR THE 1B RUN ONLY, recorded here); **2.6B DECLINED — not reachable in this harness** (no RUNGS[4]; _MAX_N_LAYERS=24 guard is a deliberate architecture boundary; two-arm pure-fp32 = 2×16B×N = 86.1 GB optimizer+param state at 2.6B, OOM at every batch size; the largest compliant candidate measured 2.69B/arm and OOMed at construction+step on batch 4). The decline is a PAPER FACT: the method's measured scale ceiling under the exactness-preserving two-arm design is ~1.31B on 80 GB parts; passing it requires mixed-precision/sharding/single-arm amendments, each re-opening exactness audits. Surplus (~700 GPU-h): n=9 thickening at 1.31B and/or symmetric n=9 at 392M — decided at the current harvest. Seventeenth injection sighting, standing pattern.
