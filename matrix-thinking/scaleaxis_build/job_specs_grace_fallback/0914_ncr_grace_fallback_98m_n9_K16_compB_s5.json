{
 "id": "0914_ncr_grace_fallback_98m_n9_K16_compB_s5",
 "lane": "NCR",
 "tier": "grace_fallback",
 "hypothesis": "Fallback seed-thickening of the K=16 stratum (see notes). NCR_KSCALING_DESIGN.md sec 7 -- the CAPABILITY-BREADTH SCALING CURVE. Cell: K=16 (d=17, the PAIR is the variable -- house precedent: K=24 is dead at d=48 and healthy at d=25), TRAINABLE-contrastive (compB recipe), seed 0. Tests whether exact in-context-written operator composition (closed-form V.K-p",
 "cmd": "test -f /home/nvidia/queue/LICENSE_SWEEP_KSCALING || { echo \"GATED: K-scaling sweep not licensed by calibration (KSCALING_AUDIT_R1 M5 / REDTEAM L2)\"; exit 3; }; mkdir -p /ephemeral/kscaling/results /ephemeral/kscaling/ckpts && cd /home/nvidia/ncr_kscaling && NCR_K=16 /home/nvidia/tdenv/bin/python3 ncr_lm_wave1_runner.py --k 16 --mode calibration --device cuda --steps 20000 --batch-size 32 --eval-batch-size 64 --warmup-steps 200 --lr 3e-4 --aux-read-loss-weight 0.5 --ortho-reg-weight 0.1 --ckpt-every 10000 --eval-every 1000 --ceiling-gpuh 6.0 --aux-loss-type contrastive+cosine --contrastive-temperature 0.07 --seed 5 --cell-id kscaling_n9_K16_compB_s5 --out /ephemeral/kscaling/results/kscaling_n9_K16_compB_s5.json --ckpt-dir /ephemeral/kscaling/ckpts/kscaling_n9_K16_compB_s5 2>&1 | tee -a /ephemeral/kscaling/results/kscaling_n9_K16_compB_s5.log",
 "gpu_h_estimate": 0.783,
 "output_dir": "/ephemeral/kscaling/results",
 "validity_check": "/home/nvidia/tdenv/bin/python3 -c \"import json; d=json.load(open('/ephemeral/kscaling/results/kscaling_n9_K16_compB_s5.json')); assert d.get('status')=='COMPLETED', d.get('status'); assert d.get('step',0)>=20000, d.get('step'); ks=d.get('kscaling') or {}; assert ks.get('K')==16, ('K', ks.get('K')); assert ks.get('d_ncr')==17, ('d_ncr', ks.get('d_ncr')); assert ks.get('d_equals_k_plus_1') is True; assert ks.get('h_top')==40, ('h_top', ks.get('h_top')); assert ks.get('deep_ladder')==[4, 9, 21, 22, 39, 40], ks.get('deep_ladder')\"",
 "notes": "GRACE FALLBACK (paper agent, 2026-09-01 evening): byte-identical to the completed spec 0119_ncr_kscaling_K16_compB_s0 modulo seed/id/cell-id. Purpose: extend the K=16 h=1 wall-excursion COMPARATOR to n=9 at this scale so the three-scale h=1 table (98M/392M/1.31B) is seed-matched to the 1.31B n=9; descriptive only, changes no adjudicated verdict (EXPERIMENT_LOG 2026-09-01 #3 M1/M3). Sorted at 09xx so it runs only when nothing earlier is pending. GATED on LICENSE_SWEEP_KSCALING sentinel (M5/L2). CANDIDATE -- NOT queue-eligible until BOTH (a) the build audit passes AND (b) the calibration gate (specs 0100/0101) returns LICENSE-SWEEP. DOUBLE-GATED by design: if the K=32 frozen calibration cell fails its deep-capability leg, this sweep is re-scoped to K<=28 rather than launched. Built by the K-scaling DESIGN+BUILD agent 2026-08-21 on the same"
}