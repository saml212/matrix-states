{
 "id": "0640_embed_ablation_probe_matrix_S",
 "lane": "embed-ablation",
 "hypothesis": "Rate/admission probe (phase A, audit F2), arm=matrix size=S. Measures real per-step wall time and the last-three-T1-evals monotonicity signal BEFORE any phase-B seeded cell is staged, mirroring 005_laneA_probe_K128_s0's own Phase-0a discipline. Not itself a trainability readout for the pre-registered decision rule (harvest() filters role='probe' records out unconditionally, F1). Feeds --check-admission (audit M4), which sizes phase B's --steps and can BLOCK phase B outright if the measured rate implies >2 GPU-h/cell or if T1 is not improving.",
 "cmd": "mkdir -p /data/embed_ablation_ckpts/matrix_S /home/nvidia/embed_ablation/results/probes && cd /home/nvidia/embed_ablation && /home/nvidia/tdenv/bin/python3 embed_ablation_rd.py --run-cell --arm matrix --size S --match P --seed 0 --steps 500 --eval-interval 100 --batch-size 64 --seq-len 512 --data-dir /data/deltanet_rd_data --corpus wikitext-mix-ext --role probe --ckpt-dir /data/embed_ablation_ckpts/matrix_S --out /home/nvidia/embed_ablation/results/probes/matrix_S.json --ceiling-gpuh 0.5",
 "gpu_h_estimate": 0.147,
 "output_dir": "/home/nvidia/embed_ablation/results/probes",
 "validity_check": "/home/nvidia/tdenv/bin/python3 -c \"import json; d=json.load(open('/home/nvidia/embed_ablation/results/probes/matrix_S.json')); assert d.get('role') == 'probe'; assert d.get('complete') is True; assert d.get('steps_completed') == 500; assert 'T1' in d.get('final_evals', {})\"",
 "notes": "PREREQ (see EMBEDDING_ABLATION_DESIGN.md S6 'Deployment'): before this cmd can run, scp matrix-thinking/src/embed_ablation_rd.py to /home/nvidia/embed_ablation/embed_ablation_rd.py on the box (neither this script nor any matrix-thinking/src/ file is deployed there by default). Corpus wikitext-mix-ext must already exist under /data/deltanet_rd_data/wikitext103_mix_eot_extended/ (meta.json + train.pt + val.pt + *_doc_offsets.pt) -- the SAME corpus dir the unrelated FROZEN_BIAS_LM_DESIGN.md fixscale-seedext cells already train on (spec id 645_laneB_392m_seedext_off_wikitext-mix-ext_s29, experiment-runs/2026-08-29_box_final_archive/queue/completed/), confirming the corpus is already materialized on the box -- id 645 there is a coincidental number from that campaign's own sequence, unrelated to this design's 0640+ ids. Formula-extrapolated estimate; --ceiling-gpuh 0.5 hard-bounds worst case. This is a PHASE-A cell -- no gating dependency (phase A has none by construction), safe to queue/run immediately once the PREREQ above is met."
}
