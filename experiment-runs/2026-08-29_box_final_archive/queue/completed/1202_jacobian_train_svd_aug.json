{
  "id": "1202_jacobian_train_svd_aug",
  "lane": "jacobian_erank",
  "hypothesis": "Retrain the svd-augmented readout (nonlinear-in-Z; phi(Z)=W_down@vec(Z) + MLP(svdvals(Z))) to the exact SS5 positive-control recipe (icml-mi-workshop-2026): gpt2-small, ProsQA, gamma=0, 6 latents, d=16, 25 epochs, batch 16, seed 1337, lr=1e-4. One of the THREE readouts the paper's SS5.3 refined hypothesis predicts erank(J)~=1 for (erank>=4 falsifies) -- notable because svd_aug explicitly exposes singular values to the optimizer, so it is the readout where the refined hypothesis is arguably most surprising if erank(J) still collapses to ~1. No surviving checkpoint (same decommissioned-pod loss as the other 3; only EXPERIMENT_LOG.md's historical result (was 'pending (training)' as of the last log entry, so its final best-accuracy number needs re-derivation from this retrain -- rank_evals/svd_aug_rankeval.json exists but has no top-level best_accuracy field visible in the excerpt read) and the rank-k ablation JSON survive).",
  "cmd": "cd /home/nvidia/jacobian_erank_exp && PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True RANK=0 LOCAL_RANK=0 WORLD_SIZE=1 MASTER_ADDR=127.0.0.1 MASTER_PORT=29603 /home/nvidia/tdenv/bin/python3 run_matrix_codi.py --mode train_matrix --dataset prosqa --readout svd_aug --gamma 0.0 --epochs 25 --results-dir /home/nvidia/jacobian_erank_exp/results/svd_aug --prosqa-train-path /home/nvidia/jacobian_erank_exp/data/prosqa_train.json --prosqa-val-path /home/nvidia/jacobian_erank_exp/data/prosqa_test.json",
  "gpu_h_estimate": 3.5,
  "output_dir": "/home/nvidia/jacobian_erank_exp/results/svd_aug",
  "validity_check": "/home/nvidia/tdenv/bin/python3 -c \"import json,os; rd='/home/nvidia/jacobian_erank_exp/results/svd_aug'; assert os.path.exists(rd+'/best_run_b_matrix.pt'), 'missing checkpoint'; d=json.load(open(rd+'/results.json')); assert 'train_matrix' in d, d.keys(); ba=d['train_matrix']['best_accuracy']; assert isinstance(ba,(int,float)) and ba>0.0, ba; fe=json.load(open(rd+'/final_eval.json')); assert isinstance(fe['final_accuracy'],(int,float)); print('OK best_acc=',ba,'final_acc=',fe['final_accuracy'])\"",
  "notes": "LAUNCH-METHOD FIX: see job 1200's notes for the full writeup -- torchrun/torch.distributed.run --standalone HANGS FOREVER on this box (c10d rendezvous binds to a bogus public-resolving hostname IP, not localhost, even with --master-addr passed explicitly). This cmd instead invokes run_matrix_codi.py directly as a plain python process with RANK/LOCAL_RANK/WORLD_SIZE/MASTER_ADDR/MASTER_PORT set by hand (verified working end-to-end on GPU 6 for the flatten readout; the launch mechanism is readout-agnostic). Unique MASTER_PORT=29603 for this job to avoid a collision if run concurrently with the other 3 training jobs in this lane on different GPU workers. GPU-h estimate flagged for audit: parent queue item guessed ~2 H100h/ceiling 3.0; the closest matched historical evidence (Round PC, same recipe, sibling readouts quadratic/bilinear_gelu/flatten) recorded 202.7-208.9 min (3.38-3.48 GPU-h) on 1xH100 -- svd_aug does one extra torch.linalg.svdvals per latent step (documented-stable backward per the paper) so wall time should be comparable, possibly marginally higher. 3.5 GPU-h is the evidence-based estimate; recommend the audit raise/drop the 3.0 ceiling. Same prosqa_train/test.json provenance as job 1200. Smoked: run_matrix_codi.py --smoke-test --readout svd_aug passed all 5 checks on this box's GPU 7, including the backward-pass grad-finiteness check that exercises svdvals' backward path."
}
