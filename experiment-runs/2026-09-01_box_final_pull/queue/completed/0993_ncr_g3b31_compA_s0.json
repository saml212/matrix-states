{
 "id": "0993_ncr_g3b31_compA_s0",
 "lane": "NCR",
 "hypothesis": "Companion-A cell of the sec G3-B30 3-cell attribution grid (unchanged by amendment A1): entity_adapter FROZEN at init, aux_loss_type=cosine (the sec G3-B17 bare-cosine aux, UNCHANGED behavior -- exercises the byte-identical legacy code path per this build's own aux_loss_type default/parity guarantee) -- isolates the FREEZE axis from the CONTRASTIVE axis. If PRIMARY (0992, frozen+contrastive+cosine) avoids collapse but companion-A (frozen+cosine-only) still collapses despite the SAME frozen-init entity_adapter, the freeze alone is insufficient and the contrastive term is the load-bearing fix (the sec G3-B26 mechanism: bare cosine's degenerate optimum is removed only by the discriminative term, not by freezing entity_adapter -- freezing the ADAPTER doesn't stop the READ o_raw itself from drifting toward whatever single point minimizes mean(1-cos(o,target)) against a frozen-but-still-structured target space, since o_raw's own encoder/query pathway is NOT frozen). If companion-A does NOT collapse either, freezing entity_adapter alone (without any contrastive term) may already be sufficient, a materially different (simpler) conclusion the PRIMARY-only design would have missed. Same architecture/steps/seed as PRIMARY (0992) and companion-B (0994) -- only aux_loss_type differs among the three at fixed freeze status per the grid's own 2x2-minus-one-cell design (frozen x {cosine, contrastive+cosine}, trainable x {contrastive+cosine} -- trainable+cosine-only was the pre-G3-B26 status quo, already run 3x as mob_g3b24_s0/s1/s2, not re-run here).",
 "cmd": "mkdir -p /home/nvidia/ncr_g3b31_contrastive/results && cd /home/nvidia/ncr_g3b31_contrastive && /home/nvidia/tdenv/bin/python3 ncr_lm_wave1_runner.py --mode calibration --device cuda --cell-id mob_g3b31_compA_s0 --steps 20000 --batch-size 32 --eval-batch-size 64 --warmup-steps 200 --lr 3e-4 --aux-read-loss-weight 0.5 --ortho-reg-weight 0.1 --aux-loss-type cosine --freeze-entity-adapter --ckpt-every 10000 --eval-every 1000 --ceiling-gpuh 6.0 --seed 0 --out results/mob_g3b31_compA_s0.json --ckpt-dir results/mob_g3b31_compA_s0_ckpts 2>&1 | tee -a results/mob_g3b31_compA_s0.log",
 "gpu_h_estimate": 1.0,
 "output_dir": "/home/nvidia/ncr_g3b31_contrastive/results",
 "validity_check": "/home/nvidia/tdenv/bin/python3 -c \"import json; d=json.load(open('/home/nvidia/ncr_g3b31_contrastive/results/mob_g3b31_compA_s0.json')); assert d.get('status')=='COMPLETED', d.get('status'); assert d.get('step',0)>=20000\"",
 "notes": "CANDIDATE -- NOT queue-eligible until audited. Prepared by the sec G3-B31 BUILD agent (2026-07-29), same patched runner as 0992 (md5 9a93198b642242f512ff8489e32b0a53). --aux-loss-type cosine here is the LEGACY sec G3-B17 code path, verified byte-identical to the pre-G3-B31 unpatched runner by this build's own in-process parity smoke (sub-test A, exact 0.0 diffs on total/ce/aux/ortho loss and o_raw). Build's own 6-step smoke for this EXACT flag combination PASSED (loss_history full_graft [10.98,10.70], finite, freeze assert held every step). Companion cell to 0992 (PRIMARY) and 0994 (companion-B) -- launch all three together or not at all, per the sec G3-B30 grid's own comparative design (a single cell's result is not independently interpretable)."
}
