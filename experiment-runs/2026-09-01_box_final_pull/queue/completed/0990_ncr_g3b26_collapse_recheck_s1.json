{
  "id": "0990_ncr_g3b26_collapse_recheck_s1",
  "lane": "NCR",
  "hypothesis": "NCR_REAL_LM_DESIGN.md sec G3-B26 THE CORRECTED ROAD item 1: 'Re-verify collapse on mob_g3b24_s1/s2 checkpoints at completion (read-only, free)' -- the read-collapse finding (recovered_frac@0.9/mean_cos saturated; retrieval24_acc at chance; o pairwise cos ~1.0; entity_adapter target space collapsed to pairwise cos ~0.996) was measured on mob_g3b24_s0 alone (n=1 checkpoint, explicitly caveated in sec G3-B26's own record); mob_g3b24_s1 (queue spec 0986, --seed 1, otherwise IDENTICAL config to s0: aux-read-loss-weight 0.5, ortho-reg-weight 0.1, 20000 steps) is a same-config replicate that tests whether the collapse is a property of the OBJECTIVE (would replicate across seeds) or a training-instance fluke (would not). This job re-runs the audit's own read-only discriminability check -- generalized to accept --ckpt-path (sec G3-B27 build deliverable 5(ii), this build's own collapse_recheck_readonly.py, built from /home/nvidia/tmp_audit_g3b25/audit_readonly_discriminability.py -- SAME math, reused via runner.eval_arm_at_hops which now (sec G3-B27 deliverable 1) carries the identical discriminability_metrics construction) -- against mob_g3b24_s1's own COMPLETED checkpoint. PRE-RUN AS PART OF THIS BUILD'S OWN SMOKE (sec G3-B27, GPU 2, 2026-07-29, --data-seed 1): h=1 recovered_frac@0.9=1.0 mean_cos=0.9966 offtarget_margin=-0.00014 retrieval24_acc=0.0469 o_pairwise_cos=0.99911 target_pairwise_cos=0.9957; h=61 recovered_frac@0.9=1.0 mean_cos=0.9978 offtarget_margin=0.00031 retrieval24_acc=0.0781 o_pairwise_cos=1.00000 target_pairwise_cos=0.99552 -- pattern-matches mob_g3b24_s0's own numbers closely (reported, not adjudicated -- this build computes no verdict, blind discipline). This CANDIDATE re-runs the SAME check as a durable, queue-tracked artifact rather than relying on this build session's own ephemeral smoke output.",
  "cmd": "mkdir -p /home/nvidia/ncr_g3b25_branch/results && cd /home/nvidia/ncr_g3b25_branch && /home/nvidia/tdenv/bin/python3 collapse_recheck_readonly.py --device cuda --ckpt-path /home/nvidia/ncr_g3b24_rebalance/results/mob_g3b24_s1_ckpts/mob_g3b24_s1.ckpt.pt --data-seed 1 --out results/g3b26_collapse_recheck_s1.json 2>&1 | tee -a results/g3b26_collapse_recheck_s1.log",
  "gpu_h_estimate": 0.02,
  "output_dir": "/home/nvidia/ncr_g3b25_branch/results",
  "validity_check": "/home/nvidia/tdenv/bin/python3 -c \"import json; d=json.load(open('/home/nvidia/ncr_g3b25_branch/results/g3b26_collapse_recheck_s1.json')); assert d.get('status')=='COMPLETED', d.get('status'); assert d.get('source_cell_id')=='mob_g3b24_s1', d.get('source_cell_id'); assert d.get('data_seed_used')==1, d.get('data_seed_used'); assert 'h=61' in d.get('per_h',{})\"",
  "notes": "CANDIDATE -- NOT queue-eligible until audited (same .CANDIDATE convention as 0988/0989). Prepared by the sec G3-B27 BUILD agent (2026-07-29). --data-seed 1 is MANDATORY and NOT the same value as 0989's --data-seed 0 -- mob_g3b24_s1 was launched with --seed 1 (verified live against /home/nvidia/queue/completed/0986_ncr_g3b24_rebalance_aux05_s1.json's own 'cmd' field), a DIFFERENT entity-token vocabulary than mob_g3b24_s0's seed=0 pool. collapse_recheck_readonly.py's own --data-seed argument has NO default and exits loudly (SystemExit) if omitted against a pre-sec-G3-B26-patch checkpoint (mob_g3b24_s1/s2 predate the patch, so they have no 'seed' field to auto-detect from) -- this build's own FIRST smoke attempt on s1 used the wrong default (0) and produced garbage (recovered_frac@0.9=0.47, mean_cos=-0.06 to -0.16, target_pairwise_cos ~0.001 -- looking like NO collapse at all, which would have been a materially wrong finding), caught by comparing against the expected s0-like pattern and confirmed by reading 0986's own deployed 'cmd'; the script was then hardened (no silent default) before this spec was written, per the build report's own account of this. 'runnable after the replicates complete' (per the dispatch brief): mob_g3b24_s1 (0986) and mob_g3b24_s2 (0987) BOTH show status COMPLETED in /home/nvidia/queue/completed/ as of this build (0987 completed DURING this build session, was still 'claimed'/running on GPU0 -- the live replicate this build was instructed never to touch -- at the start of the session) -- this candidate is therefore immediately runnable once audited, not blocked on any further wait. gpu_h_estimate (0.02) is MEASURED, not guessed: this build's own smoke run against the real s1 checkpoint (full eval_batch_size=64, all 4 hops 1/2/3/61, not a --smoke-reduced config) completed in 5s wall-clock on GPU 2, i.e. ~0.0014 GPU-h; 0.02 leaves >10x headroom for a colder/contended GPU."
}
