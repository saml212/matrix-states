# NCR K=16/K=24 2x budget+anneal probes -- 2026-07-12

Queue jobs `050`-`057` (`lane_a_budget2x_probe_jobs()`, generated by the
2026-07-12 re-gate, `matrix-thinking/queue/regate_2026-07-12.md` S2(a)):
the two probes `matrix-thinking/NOVEL_ARCH_WATERFALL.md` S11.2 priced
but did not launch -- (i) K=16 2x budget/anneal, (ii) K=24 2x
budget/anneal, 4 seeds each, 8 cells total. Full record, tables, and the
observed contrast: `matrix-thinking/NOVEL_ARCH_WATERFALL.md` S11.3.

## Contents

- `earlyln_K16_s{0,1,2,3}.json` + `.axis_c_lock.json` siblings -- K=16
  (d=32), `--steps 160000` (2x `STEPS_MAIN=80_000`), seeds 0-3.
- `earlyln_K24_s{0,1,2,3}.json` + `.axis_c_lock.json` siblings -- K=24
  (d=48), `--steps 160000`, seeds 0-3.
- `md5_manifest.txt` -- md5 of all 16 JSON files, verified byte-identical
  against the box copies at `~/ncr/results_earlyln_budget2x/` on
  `youthful-indigo-turkey` at archive time.

Source: `~/ncr/results_earlyln_budget2x/` (a separate outdir from the
main `~/ncr/results_earlyln_scale/` -- required because the script's
whole-cell skip-if-COMPLETED resume check keys only on `(K,seed)`, not
`--steps`, and the 80K-step records for K=16/K=24 already existed
COMPLETED in the main outdir).

## Headline numbers (verified against the raw JSONs by this agent)

**K=16 (d=32), 2x steps+anneal:** phase_resid_max_mean s0=0.0419
s1=0.0149 s2=0.0217 s3=0.0378 (vs the 1x reference's one converged seed,
delta=0.0441); failure_front_h s0=13, s1/s2/s3=29 (vs 13 for all 4 seeds
at 1x); recovered_at_h*(h=125)=0.0 in all 4 seeds. Gate-1 (mechanical
S11 rule): 3/4 CONVERGED + 1/4 PARTIAL (up from 1/4 CONVERGED + 3/4
PARTIAL at 1x).

**K=24 (d=48), 2x steps+anneal:** phase_resid_max_mean s0=0.9321
s1=0.5689 s2=0.5109 s3=1.2379 (1x reference range 0.4337-0.8957 -- no
improvement, 2x's max exceeds 1x's max); failure_front_h=21 (=K-3,
trivial) in all 4 seeds at both budgets; recovered_at_h*(h=189)=0.0 in
all 4 seeds. Gate-1: 0/4 CONVERGED (4/4 DEAD), same as 1x.

**GPU-h:** K16 3.2995 + K24 3.7465 = 7.0460 GPU-h total (sum of each
cell's own `/gpu_h` field), against the S11.2-priced ~3.5+~4.0 = ~7.5
GPU-h estimate.

## Verification

All 8 cells: `status=COMPLETED`, `train/step=160000`,
`blank_out/passed=True` (bit-identical, grad-exactly-zero),
`axis_c_lock_sha256` present and matching each cell's own lock file,
`eval/reducer_signature/flagged=False`. Every number in this summary and
in S11.3 was read directly from these raw JSONs by this agent (not
taken from any prior summary or script output uncritically) via two
independent extraction passes; zero discrepancies against the task
prompt's supplied figures.

K=20/K=32 1x-recipe context rows cited in S11.3 are NOT archived here --
they are pre-existing Lane-A queue cells in
`~/ncr/results_earlyln_scale/` (the main K-scaling outdir), out of
scope for this probe-wave archive.
