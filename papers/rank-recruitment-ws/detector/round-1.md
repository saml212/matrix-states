# Detector gate — round 1 — rank-recruitment-ws

Protocol per `07_final_review.md` §5 (the capacity-colm-er precedent):
two independent fresh-context Opus judges, dispatched with only the
detector-judge instructions (verbatim from
`skills/paper/prompts/detector-judge.md`) plus the 7 section files
(`sections/01_intro.tex` through `sections/07_limitations.tex`,
including the three appendices embedded in 07), told to ignore
`% <!-- evidence: Rx -->` build-metadata comments and raw LaTeX markup,
and to read nothing else. Bounded to a maximum of 2 rounds; discharge
requires both judges at $\geq$90%-human AND zero cited mechanical
tells; iteration is licensed only on cited mechanical tells, never
structural-symmetry residue. Draft state: post-final-review, S1-S3
sibling de-dup edits do not touch this tree's text (this tree is the
primary carrier — no in-tree prose changed since `05_format_audit.md`'s
four minors landed).

## Verdicts

| Judge | Classification | Percentage-human read | Cited tells |
|---|---|---|---|
| A | human-written | 100% human | none |
| B | human-written | 100% human | none |

Both judges independently read the full 7-section draft (intro through
appendices) and returned unqualified 100%-human verdicts with zero
cited tells of any kind — not even the structural-symmetry residue
(antithesis, mirrored roadmaps, templated closings) that the sibling
capacity-colm-er program's judges flagged at a 68/90 split in its own
round 1. Both judges explicitly considered and cleared the one
candidate uniform-structure signal in this draft (the
`\textbf{Bold lead-in.}` micro-paragraph device used throughout every
section) as a standard ML-conference organizational convention with
genuinely varied paragraph bodies, not a filled template — flagged as
"considered and cleared" in both reports rather than silently passed
over, matching the discipline the detector-judge prompt requires.

## Disposition

**Round result: DISCHARGED under the strict criterion.** Both judges
cleared $\geq$90%-human (100%/100%) with zero mechanical tells cited
by either. Per the pre-declared rule, this is a clean pass — no fix
cycle, no round 2. Round counter: 1 of MAX_ROUNDS=2, terminated early
by a genuine double-100% clean read (not the sibling's bounded-terminal
no-actionable-defect disposition, which required a coordinator
judgment call over a 68/90 split; this round needs none).

**Terminal state: GATE DISCHARGED, round 1, no coordinator adjudication
required.** Change-list item 2 in `07_final_review.md` §6 is closed.
