# NeurIPS 2026 workshop (measurement/eval, PENDING) — submission bundle
# ("The Instrument Is the First Suspect: Six Broken Lenses in One
# Empirical Program")

`measurement-ws-submission.tex` is the flattened single-file source
(built by `make bundle` from the modular tree one level up —
`flatten_bundle.py` inlines `\input` and strips unescaped `%` comments so
source-only annotations never reach a source upload, format-audit M1);
`measurement-ws-submission.pdf` is its compiled review build, byte-
identical to `../main.pdf` and verified to reflow to the same 4pp-body /
refs-p5 / appendix-pp6-8 layout, 0 anonymization hits, 0 comments,
0 `\input`. `neurips.sty` is the flagged `UNVERIFIED — cache fallback`
stand-in template (see `../venue-requirements.md`).

## Figure provenance

Both figures are generated by `../figures/figure_gen.py`, which loads
ONLY archived raw result files and asserts the md5 of every source
before plotting (build fails loudly if an archive changed):

- `fig1_lens_contradiction.pdf` — evidence row X2:
  `experiment-runs/2026-07-10_stage2_calibration/remetric_2p32/crosscheck_lens_verdict_output.json`
  md5 `f26a769d5c263af224c91d39bd83710b` (`flat_per_cell_table`, 62 rows
  = 37 arm3 + 25 arm2; primary-vs-crosscheck contradiction on converged
  cells).
- `fig2_tap_localization.pdf` — evidence row W4:
  `experiment-runs/2026-07-09_h2h_tap_localization/results/tap_localization_SUMMARY.json`
  md5 `0e73ee283b7208db10af5588fe1c6713` (causal zeroing + four-tap
  linear-readout gaps).

Non-figure numerical claims trace to evidence rows I1–I6, W1–W5, X1–X5,
B1–B3, N1 in `../brief.md`, each carrying its verdict record, raw
artifact path, and md5. The gauntlet's format audit independently
recomputed the headline numbers against those raws
(`../gauntlet/round-1/05_format_audit.md`); the final review re-verified
four raw pointers by md5 and recomputed every number in them a second
time (`../gauntlet/round-1/07_final_review.md` §2), catching the I2
margin defect fixed in this bundle (see checklist below).

## PI pre-submission checklist

- [ ] Title (per `../brief.md`'s thesis framing; no alternates recorded)
- [ ] Camera-ready author block (commented in the tex; review build is
      anonymized by construction — anonymization grep gates every build)
- [x] **Change 1** (final-review §6 item 1, SERIOUS): §3's residual-margin
      claim corrected from sweep[0]-only stats ("at most 1.4e-06, over
      7,000×") to the file-global max across all six audited anchor
      tables at the operative n_iter=20 ("at most 1.6e-06, over
      6,000×" — file-global max 1.5555e-06 → 6,429×; independently
      recomputed against `diag_ns_admission_result.json`
      md5:d0d940d0afcdf30af8f45497168fb297, `anchor_table_ns_sweep`, all
      6 entries). `../brief.md` row I2 updated to trace the corrected
      numbers. Rebuilt + re-verified (page-2 boundary unaffected, no
      reflow).
- [ ] **Change 2** (final-review §6 item 2, PROCESS GATE): bounded
      2-round detector discharge — see `../detector/round-1.md` +
      `round-2.md`. Round 1: 90%/99% human, 2 actionable tells cited
      (fixed, 4 prose edits, no evidence/numbers touched). Round 2 (fresh
      judges post-fix): 97%/97% human — both above the 90% floor and both
      judges' bottom-line verdicts unqualified "human-written" — but each
      still cited one soft, self-described-as-non-disqualifying residual,
      so the literal "zero mechanical tells" bar is not met. MAX_ROUNDS=2
      reached; per protocol no round 3 was run. **Escalated to the
      coordinator for the discharge judgment call** (not decided by the
      change-applier); do not declare this bundle final until that call
      is recorded.
- [ ] **Change 3** (final-review §6 item 3, PROCESS GATE — two linked
      sub-items, both still open by design as of 2026-07-11):
  - Post-Jul-11 venue confirmation: `../venue-requirements.md`'s
        "Refresh obligation" — `https://neurips.cc/Conferences/2026/Workshops`
        still 404 at the 2026-07-11 re-check (notification is Jul-11
        AoE, not yet elapsed at fetch time). When the accepted-workshop
        list lands, enumerate it for a measurement/eval/science-of-DL
        slot, pull the chosen CFP, and re-verify every format
        assumption (page limit, kit, blind, archival, deadline) in
        `../venue-requirements.md` before submission.
  - FIX-4 catalogue-table reversal decision (recorded in
        `../sections/01_intro.tex` comment and
        `../venue-requirements.md`'s refresh note; adjudicated SOUND by
        the final review §3): `tab:catalogue` currently sits in the
        appendix under the 4pp working-limit fallback. If the confirmed
        CFP allows ≥5pp main text, move it into the body per the
        recorded plan and re-run the affected render checks; if 4pp
        (or the MOSS backup), no change.
- [ ] Change 4 (optional cosmetic, render-inspection M1/M2): Fig. 1 size
      / Fig. 2(b) tick-label wording — only if figures are touched for
      other reasons; not blocking.
- [ ] Change 5 (optional placement): anchor `tab:catalogue` at the start
      of the appendix (page 6 instead of 7) — moot if Change 3 moves it
      into the body.
- [ ] Submit via the workshop's OpenReview venue (or MOSS backup if the
      primary slot does not confirm)
