# Detector gate — round 4 — mstar-colm-er

Panel: two fresh-context subagents (Opus), independent, same dispatch shape.
Draft state: post round-3 fixes, commit 6758248.

## Verdicts

| Judge | Classification | Percentage-human read |
|---|---|---|
| G | human-written | ~92% human |
| H | human-written | ~93% human |

**Round result: NOT CLEAN.** Consecutive-clean counter: 0. Round counter:
4 of MAX_ROUNDS=6. Both judges explicitly record the registered-phrase
repetition as NOT a tell this round ("quoting a frozen pre-registration
string is correct practice") — the round-3 fix landed. The scope-negation
cadence is likewise conceded as content-motivated by both.

## Cited tells (merged)

1. **"The informative results are... / The two informative facts are..."
   near-identical intro/§4 pair** (both judges; also flagged separately as
   empty-emphasis-adjacent "announcing which facts matter").
2. **"Because the baseline never [verb], the comparison [licenses/yields]
   no [memory claim]" intro/conclusion skeleton echo** (judge G).
3. **"narrower than" twice at short range in §3** (judge H).
4. Bold run-in convention (judge H, weakest, conceded conventional) — no
   action, consistent with rounds 1-3.

## Fixes applied before round 5

- Tell 1: the intro instance deleted outright (the contribution bullets
  immediately following carry the same content); the §4 instance recast
  without the announcing frame ("What the comparison yields instead is the
  contender's flat horizon curve at constant bytes, together with the
  failure of every cap to change the baseline.").
- Tell 2: conclusion skeleton broken ("A baseline that never becomes
  competent anchors no ratio, so no memory multiplier is on offer; ...").
  The memory-multiplier disclaimer content is unchanged (pin intact).
- Tell 3: second §3 instance swapped to "more modest than".

Recompiled after fixes: 0 errors, 0 `??`, 10 pages. Round 5 (fresh panel)
dispatched on the revised draft. Two rounds remain under the cap; a pass now
requires rounds 5 and 6 both clean.
