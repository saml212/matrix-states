# arXiv TRIAGE — 2026-09-01 (paper agent; Sam owns every claim)

Scope: the 7 clusters from the strategy brief, scored on (a) artifact backing
verified by me today, (b) the 3-sentence defense Sam must give cold, and (c)
claim strength. Output: ranked queue, batch 1, the compute that had to launch
before Fri Sept 5, and Will's queue. Sam has NOT yet been interrogated on any
cluster; the "defense" rows are what he must be able to say, not what he said.

## 0a. Decisions ratified by Sam + strategy session (2026-09-01 PM)

- HEADLINE RULE (permanent): positive, defensible, absolute claims lead;
  ratios only against baselines that actually trained; negative results
  are supporting texture, never the headline. A weak claim gets fixed
  with compute or reframed to the strongest true statement. No
  consolation headlines.
- NeurReps EA was NOT submitted → the tree goes to arXiv + the next
  workshop. neurreps-ea and unireps-ea MERGE (one paper, Candidate A
  headline). kwall FOLDS into the NCR scale tree.
- Cluster 4 is NOT killed: reclassified STRENGTHEN-OR-DROP. A
  parameter-matched matrix-vs-flat embedding ablation is designed and
  queued on the box's grace compute behind the K=16 wave and behind the
  recall baseline-strengthening sweep (specs 0640+).
- Recall paper: a transformer baseline-strengthening sweep (capacity ×
  training length × LR, pre-registered §1.46) is queued (specs 0600–0629)
  so a legitimate comparison exists before the comparison section is
  written.
- HF dataset is PUBLIC (Sam did it). Authorship: Samuel Larson solo unless
  Will earns it per paper. Publication name "Samuel Larson", account
  samlarson16@gmail.com + pebbleml secondary, affiliation Pebble ML, ORCID
  linked. Endorsement: Ayush Noori (cs.AI, 2510.04033) if the endorser page
  confirms, else Andres Campero; batch 1 goes cs.AI primary + cs.LG
  cross-list.
- FINAL_TABLES 98M/392M labels: recompute from raws before anything cites
  it (auditor dispatched; result in §6 below when it lands).
- x-post drafts were never on the Desktop (Sam's morning inventory);
  DATA_MAP §5 was already stale. ICML arXiv tarball now at
  `~/Desktop/Archive (backed up in Drive)/arxiv-572-submission.tar.gz`
  and in the pebbleml Drive under Pebble ML — Files/04 Research & Papers/.

## 0. What changed today (facts, all verified by me)

- Box `youthful-indigo-turkey` is ALIVE (63 d uptime, GPUs idle). Reached via
  the Mac mini over Tailscale. Brev CLI on the mini is logged out; the SSH
  tunnel still works. `brev login` on the mini is a Sam action if you want
  `brev ls` / credit readouts.
- HF preservation COMPLETE and verified server-side: 1,996 files, 3,282.6 GB
  in private dataset `Slamin/ncr-scaling-artifacts` (tiers 1a/1b/2/3 all
  present). The "private storage limit" errors in the log were transient
  retries. Risk: HF may enforce a private cap; flipping the dataset public
  removes it. Sam's call.
- 1.31B results were NOT in the repo. Pulled today: 72 result JSONs, 24 n=9
  kscaling scorings, 6 attribution cells, FINAL_TABLES.txt, depth-ext, queue
  specs/logs, HF ledger → `experiment-runs/2026-09-01_box_final_pull/`
  (commits 71944a1, be7456d; >25 MB logs SSD-only). The untracked Aug-25 1B
  harvest dir was committed with it.
- Independent recompute from the raw n=9 JSONs (my code, not the box's):
  frozen arm ≥0.90 at h_top in 9/9 seeds at every K; trainable K=32 2/9,
  K=40 0/9. Matches FINAL_TABLES' n=3 verdict structure.
- Four NEW single-seed wall excursions in the n=9 data (K24 s7 h=1, K32 s6
  h=17, K32 s7 h=36, K40 s5 h=1). Pre-registered rule: re-measure at base
  seed 31337 before calling anything. LAUNCHED and DONE today: all four
  collapse into band (0.0938→0.0508, 0.0664→0.0625, 0.0664→0.0469,
  0.0586→0.0391). Composition wall stands at n=9. The K=16 h=1 toehold now
  replicates across FIVE training seeds (s0,s2,s5,s6,s7) — stronger than the
  log's two-seed statement.
- pebbleml.com/research/ IS live with findings 01–16 (DATA_MAP's "domain
  repurposed" note is stale). Findings 17–20 exist in-repo only.
- All md5-asserting figure generators pass on the mini for clusters 1, 2, 3,
  6, 7 (mstar, capacity, neurreps-ea, rank-recruitment, measurement-ws,
  opbank, k32). Flagship builds clean, 26 pp.
- Existing portfolio: 9 Gen-2 trees under `papers/` + the ICML tree, all
  inventoried in `ra-handoff/PAPERS.md`. Seven are content-complete. Every
  venue clock they targeted has passed. arXiv is the only live target.

## 1. Cluster scorecard

Score = claim strength × artifact backing, 1–5. "Defense" = the three
sentences Sam must own: what was held fixed / what grew / what would falsify.

### C1. Constant-memory recall (08+10+11) — SCORE 5 — FLAGSHIP WORKSHOP PAPER
- Tree: `papers/mstar-colm-er/` READY, zero TODOs, figure-gen + every table
  cell md5-asserted (verified today). Archives: `2026-07-10_h2h_mstar`,
  `2026-07-10_h2h_sweep_harvest`, `2026-07-09_h2h_*`. Localization (S₀/S₁
  zeroing) archived.
- Defense: HELD FIXED params (14.05M / 14.05M / 14.44M), data, schedule,
  episode seeds, K=32, 20k steps; the state is a fixed 32,768 B. GREW context
  454→1798 tokens with the state fixed; contender ≥0.998 at every horizon
  while a param-matched flat recurrence and a param-matched transformer sit
  at chance uncapped AND at every KV budget 1×–32×, after a 4-point LR search.
  FALSIFIER: any transformer LR/arch tweak that lifts task-1 above the 0.094
  demonstration bar at matched params, or a linear probe that reads recall
  from raw state (none does; only the model's own forward decodes it).
- Hostile questions Sam must survive: (1) "Your baseline is degenerate, so you
  have no multiplier" — correct, and the paper says so; the sanctioned claim is
  baseline-non-competitive, never "N× better." (2) "Why is chance the right
  comparator for a transformer that can trivially attend back?" — the
  episode-restricted task + matched tokens; he must explain why attention
  fails here (the position-decomposition shortcut is closed by design).
  (3) Task-2 (multi-hop) is NON-DECISION-GRADE (3/9 seeds) — it stays out of
  the headline. (4) K48 stress reads chance for ALL arms — state that.
- Capacity trilogy (10) is a SEPARATE tree (`capacity-colm-er`, cleanest in
  the repo, 19 rows, citation-verification.json). Ship it as its own arXiv
  paper, not folded in. Its honesty clause: super-linear per DIMENSION,
  ~conserved per BYTE. Sam must say both.

### C2. Rank law (07+02+03) — SCORE 5 — SECOND ARXIV LEAD
- Trees: `neurreps-ea` (ACCEPT-READY, arXiv zip built), `unireps-ea` (same
  evidence, different headline), `rank-recruitment-ws` (companion, Task D/E).
  Figures regenerate from md5 archives (verified today). Archives:
  `2026-07-09_capability_*`, `2026-07-09_m3fix_*`, `2026-07-02_task_e_*`.
- Defense: HELD FIXED the readout (exact continuous recovery, never argmax),
  a P=1 single-state bottleneck verified by blank-out, zero-pad targets (the
  eye-padding rank tax was the D-AMB bug). GREW d_min 2→5 across five
  permutation groups: recruited effective rank tracks d_min at ρ=0.9747
  (tie-capped max), 19/19 in band; force-rank to d_min−1 reads 0.000 in all
  five groups and recovers at d_min 5/5; S₄ vs A₅ is TOST-equivalent.
  FALSIFIER: any cell recovering at rank d_min−1, or rank tracking
  solvability class instead of dimension.
- Hostile: "Why isn't this just Nichani/Lee/Bietti?" — argmax loophole closed
  by construction; cite 2605.05189 (novelty gate item 4). "Effective rank of
  1.88 for d_min=2 — that's 6% under; where's the noise model?" — band is
  pre-registered; he must state it. Findings 02/03 (Mar-2026 rank enrichment,
  output-head dynamics) are 288K–2.5M-param Mac-mini-era; artifacts are
  `8xh100-session1/*` JSONs only, no per-run dirs. Use as motivation
  paragraph at most, never as evidence rows.
- Decision for Sam: neurreps-ea vs unireps-ea are ONE paper with two
  headlines. Pick ONE for arXiv (recommend neurreps-ea, Candidate A title);
  rank-recruitment-ws goes up as its companion (Task D/E, exact composition).

### C3. Native composition reads (14+13 + 17–20) — SCORE 4 (5 once written)
- This is where the program's newest and largest evidence sits (98M→392M→
  1.31B, ~800 GPU-h, ~176 cells, 0 failures) and it has NO standalone paper
  tree. It lives as flagship §§5a/5b (98M/392M only; 1.31B not yet written)
  and `papers/kwall/` (the K=32 wall diagnosis, 13 pp vs 4 pp cap, no
  figures, anonymization never run, novelty-gate obligation to cite
  arXiv:2607.19390 unmet).
- Defense: HELD FIXED (K,d), recipe, steps=20k, batch, t_in, ladders, bands,
  the P1b/P0 instruments and their thresholds (pinned before any 1B number).
  GREW params 13.4× (98M→392M→1.31B): exact-write capability flat at ceiling
  (both pairwise TEST-X no-detectable-shift; 1.31B medians 0.992–0.996);
  ordering T=36/36 perfect at 1.31B; trainable arm degrades at K≥32 and a
  doubled token budget rescues it at 392M but NOT at 1.31B. FALSIFIER: the
  attribution arm (40k steps) lifting 1.31B K=32/40 trainable over 0.90 (it
  reads 0.80/0.89/0.82 and 0.67/0.63/0.74 — it does not); P0 leaving band at
  any deep hop (n=9 + 31337 re-measures: it does not).
- Hostile: (1) "Frozen arm at ceiling is a hand-built operator; the model
  learns nothing" — TRUE and it is the claim: the read path EXECUTES exact
  O(log h) composition, SGD cannot LEARN the write; he must frame it as an
  execute/learn gap, not a capability. (2) D/N = 0.14 tokens/param at 1.31B
  — under-training can only manufacture DEGRADES, so STABLE is conservative;
  he must say this unprompted. (3) The TEST-W table in FINAL_TABLES labels
  the comparison row "98M" and prints a 392M cross-check MISMATCH — Will
  recomputes TEST-W from raws before it enters any paper. (4) The 2.6B
  decline is a paper fact: two-arm fp32 OOMs at 80 GB.
- Action: write a NEW 4–6 pp tree `papers/ncr-scale/` from the finding-20
  page + FINAL_TABLES + n=9, absorbing kwall as a §"the wall's mechanism"
  subsection. kwall standalone: KILL (fold).

### C6. Measurement failure modes (12+15+09) — SCORE 4 — trust piece, batch 2
- Tree: `papers/measurement-ws/` READY, 20 evidence rows, detector discharged;
  figures regenerate (verified). Two cited logs are missing from the archive
  (stage-2 instrument-health adjudication; reasoning-link layout adjudication
  log — its RESULT JSON exists). Will locates or the rows are re-pointed.
- Defense: six incidents, one five-rule discipline; the discipline is itself
  falsifiable (shuffled-target control 0.00/0.00/0.05) and did not launder
  results (endpoint verdict unchanged, two real failures kept).
- Hostile: "This is a lab notebook, not a paper." Answer is the shuffled
  control and the two failures it left standing. `reasoning-null-moss` (15,
  the bounded null) is a separate tree; arXiv-only is the right outcome and
  takes the priority date. Finding 09 (attractor) is the `iclr-2027`
  Gen-1 tree, 17k words, frozen; absorb-vs-separate is the open flagship
  question — do not spend arXiv time on it before Sept 25.

### C7. Operator bank (16) — SCORE 2 standalone — FOLD INTO C3
- Archives `2026-07-11_ncr_opbank_*` intact; site generator md5 passes.
  Result is "architecture proven, exact contender convergence-recovered,
  far depth still open." Not a paper. One paragraph + one figure in the
  C3 tree.

### C5. Bolt-on falsification (05) — SCORE 5 (published) — arXiv POST ONLY
- Package built: `~/Desktop/arxiv-572-submission.tar.gz` (9 pp, cs.LG +
  cs.CL/cs.AI, CC BY). Blockers are Sam-only: (a) endorsement — the brief
  says the ICML paper gives standing; the playbook says he has no arXiv
  history and cs.LG needs an endorser. Only Sam knows. (b) author-name split
  (OpenReview "Samuel Larson / Pebble Machine Learning" vs package "Sam
  Larson / Pebble AI"). (c) the §6 82.03% vs §4 −1.3pp seed-variance
  question — confirm same config before posting.

### C4. Matrix embeddings / parameter efficiency (01+04) — SCORE 1 today — STRENGTHEN-OR-DROP
- Mac-mini-era, byte BPB (internal-only metric per CLAUDE.md), NOT
  param-matched (Run 18: 24M vs 2.5M), the flat arm in Run 22 died at step
  2800 "before pod died", no per-run archive dirs exist (only
  `8xh100-session1/*.json`). No defense survives "was it param-matched?"
- Sam's ruling: do not kill. A parameter-matched matrix-vs-flat embedding
  ablation (same tokenized corpus, both arms complete, T=1 and iterative
  T, 3 seeds × 2 sizes, ≤30 GPU-h, pre-registered STRENGTHEN/DROP rule in
  `matrix-thinking/EMBEDDING_ABLATION_DESIGN.md`) is queued on the box
  behind the K=16 wave and the recall sweep. Live-or-drop decision when it
  lands. Until then it is not in any batch.

### C8. good-humored (RL environment for conversational humor) — SCORE 3 — batch 2 candidate, needs its own tree
- Repo `~/Experiments/good-humored` on the mini. `paper/DRAFT.md` is the
  OLDER rejection-cascade benchmark paper (Jul 17, 10.5k words), NOT the RL
  result. The RL program (RL-C → RL-E, closed 2026-08-14, STATE.md +
  EXPERIMENT_LOG 4471–4960) has no draft yet.
- Headline per the rule (positive, absolute): a certified-reward RL stack
  (LoRA r128 on Qwen3-30B, 200 steps, instrument v2) raises certified
  retention by +0.0903 at t=6.89 on n=1000 paired fresh-seed sessions,
  cutting zero-certification sessions 40% (184→110), with every guard
  clean (no laughter-bait, no damage). Defect-cleanup/topical grounding is
  positive in every measurement (+0.015 to +0.059, direction robust,
  magnitude day-dependent).
- Supporting texture, never the headline: the taste construct did not move
  (+0.0092 vs the +0.015 bar; capacity is not the lever, takeaway 65); four
  measured grader exploits (laughter-bait, partner-echo channel +0.102/turn
  causal, MiniLM shuffle attack, verl merge bug making two "nulls" identity
  comparisons). Frame: verified-reward-stack / grader-robustness paper.
- Defense Sam must give: HELD FIXED base model, objective, seeds (paired),
  instrument v2 both arms; GREW adapter rank r32→r128 (weight motion 4×);
  FALSIFIER: a cross-day re-eval reversing the retention sign (between-day
  variance ≈ effect sizes, takeaway 58 — the +0.090 is a within-day paired
  delta and must be reported as such), or the cleanup delta vanishing under
  the strict-laughter strip.
- Hostile: "retention of what, judged by whom?" (certified objective, judge
  certification EXP-012); "your taste result is a null, so what is the
  paper?" (the reward-stack verification + exploits are the contribution;
  retention is the demonstrated gain). Artifacts: runs/rl_e/merged_200_true,
  eval6 JSONs — Will verifies they exist and recomputes the t-stat.
- Rank 9 in the queue: after batch 2, target Sept 19–25, arXiv cs.CL/cs.AI.
  Needs a tree (`papers/humor-reward-stack/`) written from STATE.md +
  the log, not from DRAFT.md.

## 2. Ranked queue

| Rank | Paper (tree) | Cluster | State | Sam blockers |
|---|---|---|---|---|
| 1 | Constant-memory recall (`mstar-colm-er`) | C1 | READY | defend §4 baseline clause; authors |
| 2 | Causal rank law (`neurreps-ea`) | C2 | ACCEPT-READY, zip built | pick A vs B headline; endorsement |
| 3 | ICML rank-blindness (Desktop tarball) | C5 | built | endorsement; name split; seed-variance check |
| 4 | Rank recruitment + exact composition (`rank-recruitment-ws`) | C2 | READY | title stamp; anonymized-code placeholder |
| 5 | Capacity frontier (`capacity-colm-er`) | C1 | READY | per-byte vs per-dim wording |
| 6 | NCR scale chapter (NEW `ncr-scale`, absorbs kwall + opbank) | C3+C7 | to write | 1.31B section; TEST-W recompute |
| 7 | Instrument is the first suspect (`measurement-ws`) | C6 | READY | two missing logs |
| 8 | Three bounds on a null (`reasoning-null-moss`) | C6 | READY | none (arXiv-only) |
| 9 | Humor reward-stack / grader robustness (new tree) | C8 | to write from good-humored STATE + log | retention pins; Will verifies eval6 |
| — | `unireps-ea` | C2 | MERGED into #2 (ratified) | — |
| — | `kwall` | C3 | FOLDED into #6 (ratified) | — |
| — | Findings 01/04 | C4 | STRENGTHEN-OR-DROP, ablation queued (0640+) | live-or-drop on results |
| — | Attractor full paper (`iclr-2027` Gen-1) / flagship | — | not an arXiv item before Sept 25 | absorb-vs-separate |

**Batch 1 (Sept 12):** #1, #2, #3. Reason: all three are built, md5-verified,
and need only Sam's defense + endorsement; #3 is the cheapest and unblocks
endorsement standing for the rest.
**Batch 2 (Sept 19):** #4, #5, #6.
**Sept 25:** #7, #8, or explicit kills.

## 3. Compute before Fri Sept 5 — status

- Re-runs needed: the four n=9 wall-excursion re-measures. LAUNCHED + DONE
  + archived (be7456d). Nothing else in the queue needs GPU: every verdict
  the papers cite is archived, and weights are on HF.
- Optional while credits exist: nothing pre-registered is open. Do not
  invent runs to burn credits.
- Migration: small files done (repo + SSD). Weights: HF dataset only (SSD
  has 41 GiB free; Tier A alone is 82 GB, so the local checkpoint pull is
  not happening). Sam decides public-vs-private on HF before the box goes.

## 4. Will's queue (arrives Thu Sept 3 eve; mornings through Sept 12)

Co-authorship-grade (real intellectual contribution):
1. Independent recompute of EVERY number in batch 1 from raw JSONs with his
   own code, not the repo's generators: mstar C1–C11 rows, neurreps 14
   rows, ICML §3–§6 tables. Report every disagreement; this program has
   caught 3 instrument bugs this way.
2. TEST-W recompute at 392M and 1.31B from `2026-09-01_box_final_pull`
   (the FINAL_TABLES row-label / MISMATCH question, C3).
3. Read each batch-1 paper as a hostile reviewer and write the 5 questions
   he would ask; sits in on Sam's whiteboard defense.
Acknowledgment-grade (logistics):
4. LaTeX: author blocks, de-anonymize camera-ready, remove draftwatermarks,
   reconcile Sam/Samuel + Pebble AI naming across all trees.
5. Citation audit: re-verify every arXiv id against the live API for batch 1
   (two fabricated entries were caught before; assume more exist).
6. Venue map: scan the 102 accepted NeurIPS-2026 workshops + ICLR-2027
   workshop list for each paper; live-verify deadlines (owned by the
   strategy session; Will executes).
7. Locate the two missing measurement-ws logs or re-point the rows.

## 5. Open questions — ANSWERED 2026-09-01 PM (see §0a)
1. Endorsement: none; Ayush Noori first, Andres Campero fallback. Gate.
2. NeurReps EA: not submitted. arXiv + next workshop.
3. HF dataset: public.
4. "Samuel Larson", Pebble ML, solo unless Will earns it per paper.
5. Merge neurreps/unireps: yes. Fold kwall: yes. C4: strengthen-or-drop.

## 7. End-of-day status, 2026-09-01 (checklist v2)

DONE: A (box checked; K=16 grace wave healthy, 16/16 staged after I added
the dropped 0479; runtime audit PASS, ETA scored 07:40 PDT Sep 2). B
(Ayush qualifies under arXiv's own rule: ≥3 cs papers 3 mo–5 yr old; the
show-endorsers page is broken for everyone). C (defense brief). F (arXiv
account samuellarson, Pebble ML, cs.AI). J (submission draft 8024707 at
Start; codes generated: cs.AI ZMXTOX, cs.LG G63UFO, emailed to
samlarson16@gmail.com; message to Ayush drafted; ICML MI-workshop
organizers as parallel ask). M (FINAL_TABLES "98M" row = label carry-over,
resolved; my n=9 recompute matches the n=3 verdict structure). N
(good-humored triaged rank 9; tree scaffolded at
~/Experiments/good-humored/paper/reward-stack/ on the mini; its artifacts
were found on the SAME Brev box at /data/good-humored/runs — 18 GB / 6,048
small files pulled to the mini and mirrored to the SSD; agent filling the
13 pending evidence rows from raws). O (merge/fold/reclassify recorded;
pushed). P (collaborator map written, banner: no outreach before batch 1).
IN FLIGHT: D (recall sweep built, audit REV-narrow: no timeouts, cost
model undercounts C2 ~2x → ≈40–60 GPU-h, circular validity check, no
CUDA probe; builder applying fixes; probe spec 0599 gates the 30). E
(embedding ablation built, round-1 REV-narrow fixed — three arms now,
20.4 GPU-h — round-2 audit running; phase A probes stage first). Grace
wave protocol audit: baseline was misstated (5/9 not 2), breach rate is
post-hoc; 10 eval-only specs queued in the GPU gap tonight to compute
the pre-registered re-measures and the 392M comparator (log 2026-09-01
#3). H, I DONE late: ORCID 0009-0006-1253-9187 created (affiliation skipped:
Pebble ML is not in ROR) and linked to the arXiv account. G is MOOT:
arXiv accounts hold exactly one email (change_email replaces it), so
samlarson16@gmail.com stays the only arXiv address. K waits on Sam
sending the Ayush email; L (whiteboard
defense) did not happen today — first item tomorrow.
LATE UPDATE (18:45 PDT): Ayush replied "Happy to endorse" and asked for
PDFs; Sam sends the ICML camera-ready only (the batch-1 drafts are
anonymized "Under review at COLM 2026" builds and must be re-built named
before anyone outside sees them — Will's queue). Recall sweep: round-2
audit PASS-STAGE-AFTER-PROBE; trim elected (drop C2×3e-4×60k → 43.4
GPU-h, conditional re-add pre-registered); 0599 probe staged; on-box
watcher `stage_after_probe.sh` stages the 27 cells only on a REPRICE PASS.
Embedding ablation: round-2 fixes applied, round-3 narrow check running;
phase-A probes stage on PASS. HF token NOT yet placed (uploader polling).
SAM ACTIONS OPEN: send Ayush the codes; finish ORCID (skip affiliation);
revoke the old HF token and place a fresh one at ~/.hf_token on the box
so grace checkpoints upload; confirm CC BY for every arXiv posting.

## 6. Pending audits (results land here)
- Runtime audit of the running 1.31B K=16 grace wave (Fable, adversarial).
- Protocol audit of the same wave + the FINAL_TABLES TEST-W label question.
- Build audits of the recall-strengthening sweep (§1.46) and the embedding
  ablation before either enters the queue.
